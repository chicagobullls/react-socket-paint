import './Canvas.scss'

import React from 'react'

type Props = {}

const Canvas = (props: Props) => {
  return (
    <div className="canvas">
      <canvas></canvas>
    </div>
  )
}

export default Canvas
